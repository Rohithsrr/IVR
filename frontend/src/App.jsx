import { Link, Navigate, Route, Routes } from 'react-router-dom';
import { useState } from 'react';
import LoginPage from './pages/LoginPage'; import DashboardPage from './pages/DashboardPage'; import ScenarioPage from './pages/ScenarioPage'; import CallsPage from './pages/CallsPage'; import SettingsPage from './pages/SettingsPage';
export default function App(){const [authed,setAuthed]=useState(!!localStorage.getItem('token')); if(!authed) return <LoginPage onLogin={()=>setAuthed(true)}/>; return <div><nav><Link to='/'>Home</Link><Link to='/scenarios'>Scenarios</Link><Link to='/calls'>Calls</Link><Link to='/settings'>Settings</Link><button onClick={()=>{localStorage.removeItem('token');setAuthed(false)}}>Logout</button></nav><Routes><Route path='/' element={<DashboardPage/>}/><Route path='/scenarios' element={<ScenarioPage/>}/><Route path='/calls' element={<CallsPage/>}/><Route path='/settings' element={<SettingsPage/>}/><Route path='*' element={<Navigate to='/'/>}/></Routes></div>}
